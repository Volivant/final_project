import React from 'react';
import ReactLoading from 'react-loading';

const Loader = () => (
	<ReactLoading type={'spinningBubbles'} color={'#949494'} height={24} width={24} />
);

export default Loader;